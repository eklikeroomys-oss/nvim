# neovim configuration

This is my personal neovim configuration. Clone to:
* Windows: %APPDATA%\\..\\Local\\
* Linux: $HOME/.config

On initial startup, plugins should be installed using Lazy.
Install LSP servers using Mason and add configuration to config/plugin_autocomplete.lua
